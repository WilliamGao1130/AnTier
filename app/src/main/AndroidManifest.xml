<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- 列出全部已安装应用供白名单/黑名单选择。
         注意：上架 Google Play 时该权限受限，需在政策/数据安全中说明用途。 -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />

    <application
        android:label="@string/app_name"
        android:theme="@style/Theme.AnTier">

        <activity
            android:name=".app.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- AIDL 控制服务：持有内核，可被其他应用绑定调用 -->
        <service
            android:name=".core.EasyTierService"
            android:exported="true"
            android:foregroundServiceType="dataSync">
            <intent-filter>
                <action android:name="com.antier.core.EasyTierService" />
            </intent-filter>
        </service>

        <!-- 本应用自己的 VpnService：创建 TUN 并交给内核 -->
        <service
            android:name=".core.AnTierVpnService"
            android:exported="false"
            android:permission="android.permission.BIND_VPN_SERVICE" />
    </application>
</manifest>
